{
  "name": "arcana-proxy",
  "version": "1.0.0",
  "description": "AI proxy — route any model through arcana",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:warm": "wrangler deploy -c wrangler.warm.jsonc",
    "types": "wrangler types"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.0.0",
    "typescript": "^5.8.0",
    "wrangler": "^4.0.0"
  },
  "dependencies": {
    "@cloudflare/containers": "^0.3.7"
  }
}
