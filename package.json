{
  "name": "arcana-proxy",
  "version": "1.0.0",
  "description": "AI proxy — route any model through arcana",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types"
  },
  "devDependencies": {
    "wrangler": "^4.0.0",
    "typescript": "^5.8.0",
    "@cloudflare/workers-types": "^4.0.0"
  }
}
